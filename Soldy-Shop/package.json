{
  "name": "soldy-shop",
  "version": "1.0.0",
  "description": "Soldy.Shop - Full-stack eCommerce checkout and payment platform",
  "scripts": {
    "dev": "cd backend && npm run dev",
    "start": "cd backend && npm start",
    "server": "cd backend && npm start",
    "build": "npm install --include=dev --prefix frontend && npm run build --prefix frontend && npm install --prefix backend",
    "postinstall": "npm install --include=dev --prefix frontend && npm run build --prefix frontend && npm install --prefix backend",
    "seed:furniture": "cd backend && npm run seed:furniture"
  }
}
